// import { autoAnimatePlugin } from '@formkit/auto-animate/vue'

export default defineNuxtPlugin((nuxtApp) => {
  // Doing something with nuxtApp
  console.log('hello from autoanimate', nuxtApp)
  // nuxtApp.vueApp.use(autoAnimatePlugin)
})
